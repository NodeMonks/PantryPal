# ===========================================
# PantryPal - Environment Variables Template
# ===========================================
# This file serves as a template for required environment variables.
# Copy this file to create your environment-specific .env files:
#
# cp .env.example .env.local          (local overrides)
# cp .env.example .env.development    (development)
# cp .env.example .env.production     (production)
# cp .env.example .env.test           (testing)
#
# ⚠️ NEVER commit .env* files (except .env.example) to version control!

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=development
PORT=5000
HOST=127.0.0.1
APP_BASE_URL=http://localhost:5000

# For production deployment:
# NODE_ENV=production
# HOST=0.0.0.0
# APP_BASE_URL=https://yourdomain.com

# ===========================================
# Database Configuration
# ===========================================
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?sslmode=require
# 
# For Neon (Production):
# Get your connection string from: https://console.neon.tech
# Example: postgresql://user:password@ep-xyz.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@localhost:5432/pantrypal

# ===========================================
# Authentication & Security
# ===========================================
# Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Use different secrets for production!
SESSION_SECRET=replace-with-secure-random-string-min-32-chars
JWT_ACCESS_SECRET=replace-with-secure-jwt-access-secret
JWT_REFRESH_SECRET=replace-with-secure-jwt-refresh-secret

# Token expiration
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d
SESSION_MAX_AGE=86400000

# Production security settings (set to true in production)
# SESSION_SECURE=true
# HELMET_ENABLED=true

# ===========================================
# Email Configuration (Nodemailer)
# ===========================================
# For Gmail:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the app password as SMTP_PASS (NOT your regular Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here
EMAIL_FROM=PantryPal <noreply@pantrypal.com>

# ===========================================
# SMS Configuration (Twilio - Optional)
# ===========================================
# Get these from: https://console.twilio.com
# 1. Account SID and Auth Token from Console Dashboard
# 2. Phone Number from Phone Numbers section (must be verified)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token-here
TWILIO_PHONE_NUMBER=+1234567890

# Note: Without these credentials, invites will still work but email/SMS won't be sent
# The system will log warnings and return the invite link in the API response only

# ===========================================
# Feature Flags
# ===========================================
ENABLE_SMS_INVITES=false
ENABLE_EMAIL_INVITES=true
ENABLE_AUDIT_LOGGING=true

# ===========================================
# Security & Rate Limiting
# ===========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
CORS_ORIGINS=http://localhost:5000

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=info
DB_LOGGING=false
