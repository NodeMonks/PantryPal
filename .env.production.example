# Production Environment Configuration Template
# Copy this to .env.production for production deployment

# Neon Database Connection String (REQUIRED)
DATABASE_URL=postgresql://your-connection-string-here

# Session Secret (REQUIRED - Generate a new one for production!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=YOUR_SECURE_SESSION_SECRET_HERE

# Node Environment (REQUIRED)
NODE_ENV=production

# Cookie Domain (REQUIRED for production)
# Set to your domain (e.g., .pantrypal.com for pantrypal.com and all subdomains)
COOKIE_DOMAIN=.yourdomain.com

# Server Configuration
PORT=5000
HOST=0.0.0.0

# HTTPS Configuration
# In production, use a reverse proxy (nginx, caddy) to handle HTTPS
# The reverse proxy should:
# 1. Handle SSL/TLS certificates (Let's Encrypt recommended)
# 2. Forward requests to your Node.js app
# 3. Set proper security headers

# Security Headers (handled by reverse proxy)
# - Strict-Transport-Security
# - X-Frame-Options
# - X-Content-Type-Options
# - Content-Security-Policy
